# fly.toml app configuration file generated for bento-client-app on 2024-01-25T11:57:45+06:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'bento-client-app'
primary_region = 'sin'

[build]

[env]
  PORT = '8080'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
