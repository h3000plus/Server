import express from 'express';
const router = express.Router();
router.post('/my-address');
router.get('/addresses');
router.get('/:my-address');
export default router;
//# sourceMappingURL=address.router.js.map